import React from 'react';
import useTitle from '../../hooks/Usetitle';

const Myreviews = () => {
    useTitle('My Reviews')
    return (
        <div>
            
        </div>
    );
};

export default Myreviews;